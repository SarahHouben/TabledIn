import React, { Component } from "react";

export default class EditSchedule extends Component {
  render() {
    return (
      <div>
        <h3>Form to update/create DayReports to be added here...</h3>
        <p>First: Select date from date picker and submit that</p>
        <p>check in database if there is a DayReport for that day.</p>
        <p>If yes, display that dayreport</p>
        <p>If no, create dayreport and tables and display dayreport</p>
      </div>
    );
  }
}
